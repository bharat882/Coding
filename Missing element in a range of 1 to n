#include<stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    int sum=n*(n+1)/2;
    int arr[n-2];
    for(int i=0;i<n-1;i++)
    {
            scanf("%d",&arr[i]);
            sum=sum-arr[i];
    }
      printf("%d",sum);
}
